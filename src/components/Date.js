// import React, { useState } from 'react'
// import DatePicker from 'react-datepicker';

function Date() {
    // const [selectedDate, setSelectedDate] = useState(null);


    return <div>
    <input type='date'/>



    {/* <DatePicker 
    selected={selectedDate}
    // change when select the date you choose 
    onChange={date => setSelectedDate(date)}
    // Change the date format and put it in the specific one that you choose
    dateFormat='dd/yyyy/MM'
    // create a button that is use to clear the date after select
    isClearable
    // create a drop down for the year 
    showYearDropdown
    scrollableMonthYearDropdown
    // use to filter days of the week that you want to display
    filterDate={date => date.getDay() !== 6 && date.getDay() !== 0}/> */}
    
    </div>
}

export default                                   Date